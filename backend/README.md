# Citefold backend

The backend implements Citefold's local FastAPI API, SQLite persistence,
ingestion jobs, retrieval, grounded chat, citation snapshots, and Notes.

Use the repository-level [README](../README.md) for setup and verification.
New configuration variables use the `CITEFOLD_*` prefix; `VCC_*` aliases remain
temporarily available for existing local workspaces.
